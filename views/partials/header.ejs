<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/4.0.0/css/bootstrap.min.css" integrity="sha384-Gn5384xqQ1aoWXA+058RXPxPg6fy4IWvTNh0E263XmFcJlSAwiGgFAW/dAiS6JXm" crossorigin="anonymous">
    <script src="https://kit.fontawesome.com/65c28df05f.js" crossorigin="anonymous"></script>
    <link href="https://fonts.googleapis.com/css2?family=Montserrat&family=Allura&family=Annie+Use+Your+Telescope&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="/stylesheets/main.css">
    <title>Danene Copping</title>
</head>
<body>
  <div class="container">
    <nav id="navProp" class="navbar fixed-top navbar-expand-lg bg-dark">
      <% if(currentUser){ %>
        <p id="signedInAs">Admin: <a href="/dashboard"><%= currentUser.username %></a> </p>
        <a id="logout" href="/logout">Logout</a>
      <% } else { %>
        <a class="navbar-brand pl-5" href="/">DCStudioArtist</a>
      <% } %>
      <button class="custom-toggler navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
      </button>
      <div class="collapse justify-content-end pr-5 navbar-collapse" id="navbarNav">
        <ul class="navbar-nav">
          <li class="nav-item" data-target="#navbarNav">
            <a class="nav-link"  href="/">Home <span class="sr-only">(current)</span></a>
          </li>
          <% if(currentUser){ %>
            <li class="nav-item" data-target="#navbarNav">
              <a class="nav-link" href="/dashboard">Dashboard</a>
            </li>
          <% } %>
          <li class="nav-item" data-target="#navbarNav">
            <a class="nav-link" href="/gallery">Galleries</a>
          </li>
          <li class="nav-item" data-target="#navbarNav">
            <a class="nav-link" href="/testimonials">Testimonials</a>
          </li>
          <li class="nav-item"  data-target="#navbarNav">
            <a class="nav-link" href="/about">About</a>
          </li>
          <li class="nav-item" data-target="#navbarNav">
            <a class="nav-link"  href="/contact">Contact</a>
          </li>
        </ul>
      </div>
    </nav>
  </div>
  <div class="alertFlash">
    <%if(error && error.length > 0){%>
    <div class="alert alert-danger"><%=error%></div>    
    <%}%>
  </div>
  <div class="alertFlash">
      <%if(success && success.length > 0){%>
      <div class="alert alert-success"><%=success%></div>   
      <%}%> 
  </div>