<%- include('partials/header') %>

<div id="contact">
  <div id="contactFooterDiv">
    <div id="contactContent" class="rounded">
      <h1>Contact 
        <% if(currentUser){ %>
        <% if(emails.length ==  0){ %>
        <a href="/contact/new"><i class="fas fa-plus"></i></a> 
        <% } %>
        <% if(emails.length == 1){ %>
        <a href="/contact/<%= emails[0]._id %>/edit"><i class="fas fa-pencil-alt"></i></a>
        <% } %>
        <% } %>
      </h1>
      <div class="headingDiv">
        <form action="/send" method="POST" enctype=”multipart/form-data”>
          <div class="form-group">
            <input class="form-control" type="text" name="fullName" placeholder="Full Name" required>
          </div>
          <div class="form-group">
              <input class="form-control" type="email" name="email" placeholder="E-mail" required>
          </div>
          <div class="form-group">
              <textarea class="form-control" type="text" name="message" placeholder="Message" required maxlength="6000" rows="6"></textarea>
          </div>
          <button class="btn btn-dark">Send</button>
        </form>
      </div>

    </div>
  </div>
  <footer id="contactFooter">
    <span><i class="far fa-copyright"></i> Danene Copping 2020 - All Rights Reserved</span>
</footer>
</div>


<%- include('partials/footer') %>